sudo: false
script: make ICEBOX=$HOME/extras/share/icebox && make test
language: cpp
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - llvm-toolchain-precise-3.6
    packages:
    - gperf
    - libftdi-dev
    - build-essential
    - clang
    - bison
    - flex
    - libreadline-dev
    - gawk
    - tcl-dev
    - libffi-dev
    - git
    - mercurial
    - graphviz
    - xdot
    - pkg-config
    - python
    - g++-4.8
    - clang-3.6
before_install:
  - printenv
  - pwd
  - echo HOME=$HOME
  - ls -F $HOME
  - if [ "$TRAVIS_OS_NAME" = "osx" ]; then brew update; brew install autoconf automake bison gawk gnu-sed libftdi libffi mercurial graphviz pkg-config; fi
  - if [ "$TRAVIS_OS_NAME" = "linux" ]; then if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi; if [ "$CXX" = "clang++" ]; then export CXX="clang++-3.6" CC="clang-3.6"; fi; fi
  - echo CC=$CC CXX=$CXX
  - mkdir -p $HOME/extras/bin
  - git clone git://github.com/steveicarus/iverilog.git
  - (cd iverilog && autoconf && ./configure --prefix=$HOME/extras && make && make install)
  - export PATH=$PATH:$HOME/extras/bin
  - git clone https://github.com/cliffordwolf/icestorm.git
  - (cd icestorm && make && make DESTDIR=$HOME/extras install)
  - git clone https://github.com/cliffordwolf/yosys.git
  - (cd yosys && make && make test && make DESTDIR=$HOME/extras install)
compiler:
  - clang
  - gcc
os:
  - linux
  - osx
